/*
Seems pointless now, but additional functionality might be needed within the caching layer.

Best to seperate them now....


Additional functionality:

- Persisiting cache

- Invalidating the cache when the clinet is redeployed

*/

import createCache from './createCache';

export default createCache;
