// Includes the filters section, used to provide more advanced filters e.g  Runtime, Genres etc
export { default as FiltersSection } from './Filters/Filters';

// Includes the Where To Watch section, used to filter resources down to certain provides e.g. Netflix, Disney (Depends on the region selected)
export { default as WhereToWatchSection } from './WhereToWatch/WhereToWatch';

// Includes the Sort By section, used to change the sort for the filters e.g. title in descending, top rated etc
export { default as SortBySection } from './SortBy/SortBy';
