import { CommandSeparator } from '~/components/command/command';

export const SelectSeparator = (props: React.ComponentPropsWithoutRef<typeof CommandSeparator>) => {
	return <CommandSeparator {...props} />;
};
