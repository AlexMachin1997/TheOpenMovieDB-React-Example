import * as React from 'react';
import * as LabelPrimitive from '@radix-ui/react-label';

/** Properties for the Label component. */
export interface ILabel extends React.ComponentProps<typeof LabelPrimitive.Root> {}
